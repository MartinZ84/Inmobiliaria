@using Inmobiliaria.Models.Entidades
@model Inmobiliaria.Models.Entidades.Contrato

@{
    ViewData["Title"] = "Contrato - Editar";
    var Inquilinos= (IList<Inquilino>)ViewBag.Inquilinos;
    var Inmuebles= (IList<Inmueble>)ViewBag.Inmuebles; 
}



<h3 class="mt-3">Editar Contrato N° @Html.DisplayFor(model => model.Id)</h3>
<small class="text-muted">
   <i class="bi bi-info-circle me-1"></i>
    Solo se puede editar el precio
</small>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" id="formContrato">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Id" class="control-label"></label>
                <input asp-for="Id" class="form-control"  disabled/>
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FechaInicio" class="control-label"></label>
                <input asp-for="FechaInicio" class="form-control" name="fechaInicio"  id="fechaInicio" readonly/>
                <span asp-validation-for="FechaInicio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FechaFin" class="control-label"></label>
                <input asp-for="FechaFin" class="form-control" name="fechaFin" id="fechaFin" readonly/>
                <span asp-validation-for="FechaFin" class="text-danger"></span>
                      
             
            </div>
            
            <div class="form-group">
                <label asp-for="Precio" class="control-label"></label>
                <input asp-for="Precio" class="form-control" />
                <span asp-validation-for="Precio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Estado" class="control-label"></label>
                <input asp-for="Estado"  class="form-control" readonly  name="Estado">
                <span asp-validation-for="Estado" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="InquilinoId" class="control-label"></label>
                @* <input asp-for="InquilinoId" class="form-control" /> *@
                <select class="form-select" id="InquilinoId" disabled>
                    @foreach (var item in Inquilinos)
                    {
                        @:<option @(item.Id==Model.InquilinoId ? "selected" : "") value="@item.Id">@item.Nombre @item.Apellido</option>
                    }
                </select>
                <input type="hidden" name="InquilinoId" value="@Model.InquilinoId" />
                <span asp-validation-for="InquilinoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="InmuebleId" class="control-label"></label>
                @* <input asp-for="InmuebleId" class="form-control" /> *@
                <select class="form-select" name="InmuebleId" id="InmuebleId" disabled>
                    @foreach (var item in Inmuebles)
                    {
                        @:<option @(item.Id==Model.InmuebleId ? "selected" : "") value="@item.Id">@(item.Id + " " + item.Direccion)</option>
                    }
                </select>
                <input type="hidden" name="InmuebleId" value="@Model.InmuebleId" />
                <span asp-validation-for="InmuebleId" class="text-danger"></span>
            </div>
           
            <br>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />                
                <a asp-action="Index" class="btn btn-secondary">Volver al listado</a> |
                <a asp-action="Revocar" asp-route-id="@Model.Id" class="btn btn-secondary">Revocar contrato</a>
            </div>
        </form>
    </div>
</div>
<br>

<script>
      $(document).ready(function () {
            // -------------------------------
            // 1. Validar InquilinoId > 0
            // -------------------------------
            $("#InquilinoId").on("change", function () {
                validarInquilino();
            });

            function validarInquilino() {
                let valor = $("#InquilinoId").val();
                if (!valor || valor == "0") {
                    mostrarError("#InquilinoId", "Debe seleccionar un inquilino");
                    return false;
                } else {
                    limpiarError("#InquilinoId");
                    return true;
                }
            }

            // -------------------------------
            // 2. Validar InmuebleId > 0
            // -------------------------------
            $("#InmuebleId").on("change", function () {
                validarInmueble();
            });

            function validarInmueble() {
                let valor = $("#InmuebleId").val();
                if (!valor || valor == "0") {
                    mostrarError("#InmuebleId", "Debe seleccionar un inmueble");
                    return false;
                } else {
                    limpiarError("#InmuebleId");
                    return true;
                }
            }

            // -------------------------------
            // 3. Validar fechaInicio >= hoy
            // -------------------------------
            @* $("#fechaInicio").on("change", function () {
                validarFechaInicio();
            });

            function validarFechaInicio() {
                let valor = $("#fechaInicio").val();
                if (!valor) return false;

                let hoy = new Date().toISOString().split("T")[0]; // fecha de hoy en formato "2025-09-02"
                
                if (valor < hoy) {
                    mostrarError("#fechaInicio", "La fecha de inicio no puede ser menor a hoy");
                    return false;
                } else {
                    limpiarError("#fechaInicio");
                    return true;
                }
            } *@

            // -------------------------------
            // 4. Validar fechaFin > fechaInicio
            // -------------------------------
            $("#fechaFin").on("change", function () {
                validarFechaFin();
            });

            function validarFechaFin() {
                let inicio = $("#fechaInicio").val();
                let fin = $("#fechaFin").val();
                if (!inicio || !fin) return false;

                let fechaInicio = new Date(inicio);
                let fechaFin = new Date(fin);

                if (fechaFin <= fechaInicio) {
                    mostrarError("#fechaFin", "La fecha de fin debe ser mayor a la fecha de inicio");
                    return false;
                } else {
                    limpiarError("#fechaFin");
                    return true;
                }
            }

            // -------------------------------
            // Validación antes de enviar
            // -------------------------------
            $("#formContrato").on("submit", function (e) {
                let okInquilino = validarInquilino();
                let okInmueble = validarInmueble();
                @* let okInicio = validarFechaInicio(); *@
                let okFin = validarFechaFin();

                if (!(okInquilino && okInmueble && okFin)) {
                //&& okInicio 
                    e.preventDefault(); // detener submit
                }
            });

            // -------------------------------
            // Helpers para mensajes de error
            // -------------------------------
            function mostrarError(selector, mensaje) {
                let span = $(selector).closest(".form-group").find(".text-danger");
                span.text(mensaje);
            }

            function limpiarError(selector) {
                let span = $(selector).closest(".form-group").find(".text-danger");
                span.text("");
            }
        });

</script>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
